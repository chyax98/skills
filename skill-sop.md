# Skill 构建 SOP（评估 → 拆解 → 创建 → 调优）

## 0. Skills 是什么（补充概览）
- 可重用、基于文件系统的资源包，按需加载，避免在多轮对话重复提供指令。
- 作用：为 Claude 提供领域专长（流程、上下文、最佳实践），把通用代理转成专家。
- 优势：专业化 Claude；一次创建、自动复用；可组合多个 Skill 构建复杂流程。
- 预构建与自定义：Anthropic 提供文档类预置 Skills（pptx/xlsx/docx/pdf）；自定义 Skills 同构，适用于 Claude Code/API/Claude.ai（各平台独立上传/发现）。
- 文件系统与 VM：Claude 在具备文件访问和代码执行的容器中，像新同事看入职包一样读取指令/运行脚本。
- 渐进式披露（三级加载）：
  - Level 1 元数据：`name/description` 启动时常驻，约百 tokens。
  - Level 2 指令：触发 Skill 时读取 `SKILL.md` 主体，<5k tokens。
  - Level 3 资源：按需读取/执行 `references/`、`scripts/`、`assets/`，文件本身不进上下文；脚本仅输出占用 tokens。

## 1. 判断任务是否值得做成 Skill
- **触发频率/可重复性**：重复出现的需求、标准化流程、常被遗忘的细节。
- **模型盲区**：与通用大模型知识差距大的专有流程/业务规则/格式要求。
- **脆弱性/一致性需求**：易出错、需强约束或确定性代码的操作（低自由度）。
- **资源复用价值**：可沉淀脚本、模板、参考文档、资产供多次使用。
- **上下文成本**：放进 SKILL.md 的信息必须是 Claude 不自带、且值得占用上下文的内容。

不适合：一次性、极简线性操作；缺乏可复用资产；无专有知识需求。

## 2. 从任务到 Skill 的拆解方法
1) **收集具体使用场景**（Skill Creator Step 1）
   - 用户会说什么话触发它？期望产出是什么？举 3–5 个真实例子。
2) **逐例分析可复用要素**（Step 2）
   - 需要哪些脚本？哪些参考资料/接口文档？是否需要输出模板/字体/图形资产？
   - 判断每个信息应放哪一层（元数据 / SKILL.md / references / assets / scripts）。
3) **确定自由度**
   - 高自由：文本指导；中自由：伪代码+参数；低自由：固定脚本/严格步骤。
4) **列出资源包**
   - `scripts/`：重复性强、需确定性的代码。
   - `references/`：长文档/规范/Schema/接口说明，按需加载避免占用上下文。
   - `assets/`：直接被产出使用的模板、图片、字体等，不进上下文。

## 3. Skill 结构与要求
- **目录**：`skill-name/` 必含 `SKILL.md`，可选 `scripts/`, `references/`, `assets/`。
- **SKILL.md Frontmatter（必填）**
  - `name`: 与目录同名、kebab-case、小写字母数字+连字符。
  - `description`: 清晰描述做什么、何时触发（用第三人称：“该技能用于…”）。
- **Frontmatter 可选**
  - `license`: 简短标识（或指向随附 license 文件）。
  - `allowed-tools`: 预批准的工具列表（Claude Code 支持）。
  - `metadata`: 额外键值。
- **正文写法**
  - 采用祈使/不定式，客观指令；避免“你/我”。
  - 仅放必要流程与调用方式；详尽资料放 references。
  - 三层渐进加载：元数据（总在）、SKILL.md（触发时）、资源（按需）。
- **不要放**：README、安装指南、变更日志等与执行无关的杂项文档。

## 4. 标准创建流程
1) **初始化**：运行 `scripts/init_skill.py <skill-name> --path <output-dir>` 生成骨架与占位文件。
2) **填充资源**：替换/新增 `scripts/`、`references/`、`assets/` 中确定需要的内容，删除无用示例。
3) **编写 SKILL.md**
   - 明确目的、触发场景、步骤；引用资源的用法。
   - 复杂多步流程时应用 **DEPTH 框架**：
     - D 角色分工：3–5 个专家角色覆盖关键视角。
     - P 背景收集：Step 0 收集 4–6 个关键信息（受众/输入范围/质量要求/时间等）。
     - T 任务分解：对复杂步骤列 4–7 个子任务。
     - E 成功标准：最终步骤列 4–6 条可验证标准。
     - H 自检环节：交付前按类别检查，并给出不合格时的修正动作。
4) **验收与调优**
   - 用真实任务试跑；记录瓶颈 → 回写 SKILL.md 或补充资源。
   - 关注：触发误判、上下文过长、角色/步骤缺失、成功标准不可验证。
5) **维护**
   - 版本迭代时，保持元数据精确；更新 references/脚本时同步调整正文引用。

## 5. 常用写作片段（可直接贴入）
- **Frontmatter 模板**
  ```markdown
  ---
  name: my-skill-name
  description: 该技能用于……（何时触发 + 产出）
  license: Apache-2.0
  allowed-tools: []
  metadata: {}
  ---
  ```
- **多角色协作（DEPTH-D）**
  - Content Architect: 结构设计
  - Domain Researcher: 事实/资料收集
  - Implementer: 生成/执行核心步骤
  - QA Reviewer: 对照成功标准自检
- **成功标准（示例）**
  - 需求覆盖、格式合规、关键风险标注、可执行性/可复现性、长度/上下文限制。

## 6. 许可证与合规提示
- 在 `license` 标注所用许可证；若附带第三方代码/资产，保持相应 NOTICE/许可证文本随附，并在 SKILL.md 或资源文件夹中引用。
- 对 GPL/字体/OFL 等受限资产，确认用途是否合规；避免把不需加载的长许可文本放入上下文，可作为独立文件留存。

## 7. 快速复盘清单
- 任务是否具备重复价值和专有知识？  
- 资源是否分类进 scripts/references/assets，避免冗余？  
- SKILL.md 是否简洁、祈使式、第三人称？  
- 是否包含触发条件、步骤、资源引用方式和成功标准？  
- 复杂流程是否套用 DEPTH（D/P/T/E/H）？  
- 许可证与第三方归属是否标注清楚？

